<Window x:Class="SSLapp.Views.UpdateCompleteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SSLapp.Views"
        mc:Ignorable="d"
        Title="Setup Complete" Height="450" Width="800"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignLightBackground}"
        TextElement.FontWeight="Light"
        TextElement.FontSize="13"
        FontFamily="{materialDesign:MaterialDesignFont}" d:DesignWidth="400" d:DesignHeight="200">
    <Grid>
        <DockPanel Height="180">
            <Button  IsDefault="True" Content="{Binding UpdateCompleteModel.Button, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=False}" 
                 Margin="15,15,15,15" 
                 Width="80" 
                 Foreground="White"
                 HorizontalAlignment="Right"
                 VerticalAlignment="Bottom"
                 Command="{Binding AcceptCommand}"/>
        </DockPanel>
    </Grid>
</Window>
